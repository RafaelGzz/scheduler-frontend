import 'package:flutter/material.dart';

class PtosPage extends StatelessWidget {
  const PtosPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
